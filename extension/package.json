{
  "name": "flow-harvester-extension",
  "version": "2.0.0-spike.1",
  "private": true,
  "description": "Flow Harvester V2 Chrome extension (spike). Loads via chrome://extensions → Load unpacked → select dist/.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch --mode development",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .vite"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
